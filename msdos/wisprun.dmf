#
# makefile for the wisp library on MSDOS platforms.
# Builds the WISPRUN.EXE WISP run-time EXECUTABLE MODULE.
#

RTSRC=WISPRUN.COB
RTX=WISPRUN.GNT
USERTX=E:\WISPBIN\$(RTX)

all: $(USERTX)

$(USERTX):      $(RTX)
	COPY $(RTX) $(USERTX)

$(RTX): $(RTSRC)
	COBOL $(RTSRC) /NOANIM /GNT /OMF "GNT" /OPT "1"; 

